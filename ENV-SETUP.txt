Create a .env file in aegis-ao-rental-admin directory with:

REACT_APP_API_URL=https://localhost:7163/api

IMPORTANT: The /api at the end is required!

After creating the .env file, restart the React dev server (npm start)

